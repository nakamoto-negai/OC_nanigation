# HTTPS(443) はベースの docker-compose.yml に統合されました。
#
# もう override 指定は不要で、次のコマンドだけで常に HTTPS(443) が有効になります:
#   docker compose up -d --build
#
# このファイルは後方互換（既存の手順・スクリプトが -f docker-compose.https.yml を
# 付けて呼んでも壊れないように）のために、何も上書きしない空 override として残しています。
services: {}
